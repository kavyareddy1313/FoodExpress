apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: foodexpress
  labels:
    app: foodexpress
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: foodexpress
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
